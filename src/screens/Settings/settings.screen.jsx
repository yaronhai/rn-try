import { Text } from "react-native";
import { SettingsContainer, SettingsTitle } from "./settings.styles";

export const SettingsScreen = () => {
    return(
        <SettingsContainer>
            <SettingsTitle> Settings </SettingsTitle>
        </SettingsContainer>
    )
};